# All MP3s are regenerable. Only out/final.mp4 (the rendered deliverable)
# stays in version control; everything else can be reproduced via
# voiceover.py + the ffmpeg recipes in workflows/phase-5-audio.md and
# example/README.md § "Reproducing the render".
*.mp3

# Transcript is regenerated by `npx hyperframes transcribe` / Whisper
transcript.json
voiceover.srt
voiceover.txt
voiceover.tsv
voiceover.vtt
